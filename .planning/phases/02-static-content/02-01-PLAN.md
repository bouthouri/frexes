---
phase: 02-static-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - src/components/Hero.astro
autonomous: true
requirements:
  - HERO-01
  - TECH-02

must_haves:
  truths:
    - "Hero section shows 'Frexes' name and tagline above the fold"
    - "SEO meta tags and OG tags present in page source"
    - "Inter font loads from Google Fonts (not system fallback)"
  artifacts:
    - path: "src/components/Hero.astro"
      provides: "Hero section with name + tagline"
      min_lines: 10
    - path: "src/layouts/BaseLayout.astro"
      provides: "Full SEO/OG meta tags + Inter font link"
      contains: "og:title"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "Google Fonts"
      via: "link preconnect + stylesheet"
      pattern: "fonts\\.googleapis\\.com.*Inter"
---

<objective>
Hero section with name/tagline + BaseLayout SEO/OG wiring + Inter font loading.

Purpose: Visitors see "Frexes" identity above the fold. Search engines and social platforms get proper meta tags.
Output: Hero.astro component, updated BaseLayout.astro with OG/SEO tags and font loading.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-content/02-RESEARCH.md

<interfaces>
<!-- Existing BaseLayout interface -->
From src/layouts/BaseLayout.astro:
```astro
interface Props {
  title?: string;
  description?: string;
}
```

From src/styles/global.css:
```css
--font-sans: "Inter", ui-sans-serif, system-ui, sans-serif;
--font-heading: "Inter", ui-sans-serif, system-ui, sans-serif;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BaseLayout SEO/OG meta tags + Inter font loading</name>
  <files>src/layouts/BaseLayout.astro</files>
  <action>
Update BaseLayout.astro Props interface to add `url` and `image` optional props with defaults:
- title = "Frexes"
- description = "Building apps that actually help you get things done"
- url = "https://frexes.dev"
- image = "https://frexes.dev/og.png"

Add to `<head>` BEFORE the title tag:
1. Google Fonts preconnect + Inter stylesheet link (weights 400,500,600,700, display=swap)
2. Open Graph tags: og:type=website, og:url, og:title, og:description, og:image
3. Twitter Card tags: twitter:card=summary_large_image, twitter:title, twitter:description, twitter:image
4. Canonical link: rel=canonical href={url}

Keep existing favicon links, charset, viewport, description, generator meta tags.
Do NOT install astro-seo — hand-write the ~10 meta tags directly.
  </action>
  <verify>
    <automated>cd /Users/bouthouri/work/frexes && bun build 2>&1 | tail -5</automated>
  </verify>
  <done>BaseLayout has OG/Twitter/canonical meta tags in head, Inter font loads via Google Fonts link, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Hero section component</name>
  <files>src/components/Hero.astro</files>
  <action>
Create `src/components/Hero.astro` — pure Astro component, no React, no client directives.

Content:
- `<section>` wrapper with centered layout, generous vertical padding (py-24 sm:py-32 lg:py-40), max-w-4xl mx-auto, px-4 sm:px-6
- `<h1>` with "Frexes" — text-5xl sm:text-6xl lg:text-7xl, font-bold, font-heading, tracking-tight
- `<p>` with tagline "Building apps that actually help you get things done" — text-lg sm:text-xl, text-muted-foreground, mt-4 sm:mt-6, max-w-2xl
- Center text on all viewports (text-center)
- Use flex flex-col items-center for centering

Do NOT add any animations or motion imports — Phase 3 handles that.
Do NOT use any React or client:* directives.
  </action>
  <verify>
    <automated>cd /Users/bouthouri/work/frexes && bun build 2>&1 | tail -5</automated>
  </verify>
  <done>Hero.astro exists with h1 "Frexes" and tagline paragraph, responsive text sizing, centered layout</done>
</task>

</tasks>

<verification>
- `bun build` passes with zero errors
- View source of built index.html contains og:title, og:description, twitter:card meta tags
- Hero section renders "Frexes" heading and tagline
</verification>

<success_criteria>
- BaseLayout has complete OG + Twitter Card + canonical meta tags
- Inter font loads via Google Fonts (preconnect + stylesheet link)
- Hero.astro shows "Frexes" name + tagline with responsive typography
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-content/02-01-SUMMARY.md`
</output>
