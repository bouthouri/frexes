---
phase: 02-static-content
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/components/About.astro
  - src/components/Footer.astro
  - src/pages/index.astro
autonomous: false
requirements:
  - ABOUT-01
  - FOOT-01
  - TECH-01

must_haves:
  truths:
    - "About section with indie maker identity is readable"
    - "Footer shows X (@bouthouri101) and LinkedIn links"
    - "All sections stack correctly on mobile (375px), tablet (768px), and desktop (1440px)"
    - "External social links open in new tab"
  artifacts:
    - path: "src/components/About.astro"
      provides: "About section with brief indie maker copy"
      min_lines: 10
    - path: "src/components/Footer.astro"
      provides: "Footer with X and LinkedIn social links"
      min_lines: 10
    - path: "src/pages/index.astro"
      provides: "Full page composing Hero + AppsGrid + About + Footer"
      contains: "AppsGrid"
  key_links:
    - from: "src/pages/index.astro"
      to: "all section components"
      via: "Astro imports + composition"
      pattern: "import.*(Hero|AppsGrid|About|Footer)"
    - from: "src/components/Footer.astro"
      to: "external social URLs"
      via: "a tags with target=_blank"
      pattern: "twitter\\.com|x\\.com|linkedin\\.com"
---

<objective>
About section, Footer with social links, and full page composition with responsive QA.

Purpose: Complete all static content sections. Wire everything into index.astro. Verify responsive layout across viewports.
Output: About.astro, Footer.astro, updated index.astro composing all sections.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-content/02-RESEARCH.md
@.planning/phases/02-static-content/02-01-SUMMARY.md
@.planning/phases/02-static-content/02-02-SUMMARY.md

<interfaces>
<!-- Components created by Plan 01 and 02 -->
From src/components/Hero.astro: No props, self-contained section
From src/components/AppsGrid.astro: No props, imports apps.ts internally
From src/layouts/BaseLayout.astro:
```astro
interface Props {
  title?: string;
  description?: string;
  url?: string;
  image?: string;
}
```

<!-- Existing index.astro structure to replace -->
From src/pages/index.astro:
```astro
<BaseLayout>
  <main>...</main>
  <LazyMotionProvider client:visible>
    <!-- Phase 2/3 animated components will go here -->
  </LazyMotionProvider>
</BaseLayout>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: About section + Footer components</name>
  <files>src/components/About.astro, src/components/Footer.astro</files>
  <action>
**About.astro:**
Pure Astro component. `<section id="about">` wrapper.
- Container: `mx-auto max-w-3xl px-4 py-16 sm:px-6 lg:px-8`
- `<h2>`: "About" — text-2xl sm:text-3xl font-bold font-heading
- 2-3 short paragraphs, confident first-person indie maker voice. Content along these lines:
  - "I'm an indie developer building apps that help people get things done. No bloat, no feature creep — just tools that work."
  - Brief mention of focus on quality and craft.
  - Something about shipping real products to real users.
- Paragraph styling: `text-muted-foreground mt-4 leading-relaxed`
- Keep copy concise — 3-4 sentences total across paragraphs.

**Footer.astro:**
Pure Astro component. `<footer>` wrapper.
- Container: `mx-auto max-w-5xl px-4 py-8 sm:px-6 lg:px-8`
- Flex layout: `flex flex-col items-center gap-4 sm:flex-row sm:justify-between`
- Left: copyright — `<p class="text-sm text-muted-foreground">` with current year and "Frexes"
- Right: social links row with gap-4
  - X (Twitter): link to `https://x.com/bouthouri101`, inline SVG icon for X logo (simple path), `target="_blank" rel="noopener noreferrer"`
  - LinkedIn: link to `https://linkedin.com/in/bouthouri`, inline SVG icon for LinkedIn logo, same target/rel attributes
  - Icon sizing: w-5 h-5, text-muted-foreground hover:text-foreground transition-colors
- Add a subtle top border: `border-t border-border` on the footer element

Do NOT use any icon library — inline the SVG paths directly (X logo is a simple path, LinkedIn is the "in" square).
  </action>
  <verify>
    <automated>cd /Users/bouthouri/work/frexes && bun build 2>&1 | tail -5</automated>
  </verify>
  <done>About.astro has indie maker copy, Footer.astro has X and LinkedIn links with inline SVG icons, both build cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Compose all sections in index.astro</name>
  <files>src/pages/index.astro</files>
  <action>
Replace current index.astro content. Import and compose all sections:

```astro
---
import BaseLayout from "../layouts/BaseLayout.astro";
import Hero from "../components/Hero.astro";
import AppsGrid from "../components/AppsGrid.astro";
import About from "../components/About.astro";
import Footer from "../components/Footer.astro";
import { LazyMotionProvider } from "../components/motion/LazyMotionProvider";
---
<BaseLayout>
  <main>
    <Hero />
    <AppsGrid />
    <About />
  </main>
  <Footer />

  <!-- Animation island: reserved for Phase 3 -->
  <LazyMotionProvider client:visible>
  </LazyMotionProvider>
</BaseLayout>
```

Key points:
- `<main>` wraps content sections (Hero, AppsGrid, About) for semantic HTML
- Footer is outside `<main>` (standard HTML5 pattern)
- Keep LazyMotionProvider at bottom for Phase 3 — it renders nothing now but maintains the hydration pattern
- Remove the old placeholder `<h1>Frexes</h1>` and centering flex
  </action>
  <verify>
    <automated>cd /Users/bouthouri/work/frexes && bun build 2>&1 | tail -5</automated>
  </verify>
  <done>index.astro composes Hero + AppsGrid + About + Footer inside BaseLayout, build passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional review of all static content</name>
  <files>src/pages/index.astro</files>
  <action>
Human verifies the complete static content build across all plans (01, 02, 03). No automated work in this task — all implementation done in prior tasks.
  </action>
  <verify>
    <automated>cd /Users/bouthouri/work/frexes && bun build 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirms all sections render correctly, links work, responsive layout is correct, OG tags present</done>
  <what-built>All static content sections: Hero (name + tagline), App Grid (4 cards with badges and links), About (indie maker copy), Footer (X + LinkedIn links). Responsive layout across viewports. SEO/OG meta tags in page source.</what-built>
  <how-to-verify>
    1. Run `bun dev` and open http://localhost:4321
    2. Verify Hero shows "Frexes" heading + tagline centered
    3. Scroll to app grid — 4 cards with icons, names, taglines, badges
    4. Click Energy XP card — should open https://energyxp.app in new tab
    5. Click Progres card — should open https://progres.ing in new tab
    6. Voila and Karv cards should NOT be clickable links
    7. Voila/Karv show "Coming Soon" badge, Energy XP/Progres show "Live"
    8. About section has readable indie maker copy
    9. Footer has X and LinkedIn icon links (click to verify they open correct profiles)
    10. Resize browser to ~375px width — all sections stack in single column, readable
    11. Resize to ~768px — app grid shows 2 columns
    12. View page source (Cmd+U) — verify og:title, og:description, twitter:card meta tags present
    13. Check that Inter font loads (inspect any text element, computed font should show "Inter")
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `bun build` passes with zero errors
- All 4 sections visible: Hero, AppsGrid, About, Footer
- Responsive: single column on mobile, 2-col grid on tablet+
- External links (app URLs, social links) open in new tabs
- OG/SEO meta tags present in page source
</verification>

<success_criteria>
- Complete single-page portfolio with all static content
- Hero + App Grid + About + Footer composed in index.astro
- Mobile-first responsive design works at 375px, 768px, 1440px
- All external links have target="_blank" rel="noopener noreferrer"
- Human approves visual/functional review
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-content/02-03-SUMMARY.md`
</output>
