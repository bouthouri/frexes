---
phase: 04-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - src/styles/global.css
  - public/og.png
  - package.json
autonomous: true
requirements:
  - TECH-03
must_haves:
  truths:
    - "Google Fonts external requests are completely removed"
    - "Inter font loads from local files via @fontsource/inter"
    - "OG image exists at public/og.png (1200x630)"
    - "Lighthouse Performance score is 95+ on production build"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Self-hosted font imports, no Google Fonts links"
      contains: "@fontsource/inter"
    - path: "public/og.png"
      provides: "OG image for social sharing"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "@fontsource/inter"
      via: "CSS import in frontmatter"
      pattern: "import.*@fontsource/inter"
---

<objective>
Optimize performance to hit Lighthouse 95+ by self-hosting Inter font and creating OG image.

Purpose: Remove render-blocking Google Fonts requests (the primary Lighthouse risk) and fix missing OG image 404.
Output: Production build passing Lighthouse 95+ performance score.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-launch/04-RESEARCH.md

@src/layouts/BaseLayout.astro
@src/styles/global.css
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Self-host Inter font and create OG image</name>
  <files>package.json, src/layouts/BaseLayout.astro, public/og.png</files>
  <action>
1. Install fontsource: `bun add @fontsource/inter`

2. In `src/layouts/BaseLayout.astro` frontmatter, add imports for the 4 used weights:
   ```
   import "@fontsource/inter/400.css";
   import "@fontsource/inter/500.css";
   import "@fontsource/inter/600.css";
   import "@fontsource/inter/700.css";
   ```

3. REMOVE all 3 Google Fonts lines from the `<head>`:
   - `<link rel="preconnect" href="https://fonts.googleapis.com" />`
   - `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />`
   - `<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />`
   Also remove the `<!-- Google Fonts: Inter -->` comment.

4. Create `public/og.png` (1200x630) programmatically using a Node/Bun canvas script or satori. Design: white background, "Frexes" in bold Inter, tagline "Building apps that actually help you get things done" below, subtle gradient accent line matching hero-gradient colors (#6366f1, #ec4899, #22c55e, #f59e0b). Keep it minimal and clean. If satori/canvas is too complex, use a simple SVG-to-PNG approach or create a minimal placeholder.

5. Verify build succeeds: `bun run build`
  </action>
  <verify>
    <automated>bun run build && grep -L "fonts.googleapis.com" src/layouts/BaseLayout.astro && test -f public/og.png && echo "PASS"</automated>
  </verify>
  <done>Google Fonts links fully removed from BaseLayout, @fontsource/inter installed and imported for weights 400/500/600/700, OG image exists at public/og.png, build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Lighthouse audit — confirm 95+ performance</name>
  <files>package.json</files>
  <action>
1. Install Lighthouse CLI: `bun add -d lighthouse`

2. Build the site: `bun run build`

3. Start preview server in background: `bun run preview &` (serves on localhost:4321)

4. Run Lighthouse 3 times against http://localhost:4321 with:
   `npx lighthouse http://localhost:4321 --only-categories=performance --output=json --output-path=./lighthouse-report.json --chrome-flags="--headless=new --no-sandbox"`

5. Parse the JSON report — extract `categories.performance.score` (multiply by 100). Must be >= 95.

6. If score is below 95, diagnose the top opportunities from `audits` and fix (likely candidates: image optimization, unused JS). Re-run until 95+ achieved.

7. Kill the preview server. Clean up lighthouse-report.json (don't commit it).

8. Do NOT add a lighthouse script to package.json — this is a one-time audit.
  </action>
  <verify>
    <automated>bun run build && (bun run preview &) && sleep 3 && npx lighthouse http://localhost:4321 --only-categories=performance --output=json --quiet --chrome-flags="--headless=new --no-sandbox" 2>/dev/null | node -e "const d=require('fs').readFileSync('/dev/stdin','utf8');const s=JSON.parse(d).categories.performance.score*100;console.log('Score:',s);process.exit(s>=95?0:1)" && kill %1 2>/dev/null; echo "DONE"</automated>
  </verify>
  <done>Lighthouse Performance score is 95 or above on production build. Score logged and confirmed.</done>
</task>

</tasks>

<verification>
- `bun run build` succeeds with no errors
- No references to `fonts.googleapis.com` anywhere in source
- `public/og.png` exists and is 1200x630
- Lighthouse Performance score >= 95 on `astro preview`
</verification>

<success_criteria>
Lighthouse Performance score 95+ confirmed on production build. Font loading is fully self-hosted. OG image present.
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch/04-01-SUMMARY.md`
</output>
