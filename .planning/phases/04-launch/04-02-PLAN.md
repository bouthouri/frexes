---
phase: 04-launch
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - vercel.json
autonomous: false
requirements:
  - TECH-04
user_setup:
  - service: vercel
    why: "Static site hosting and deployment"
    env_vars: []
    dashboard_config:
      - task: "Import GitHub repo in Vercel dashboard"
        location: "vercel.com/new -> Import Git Repository"
      - task: "Add frexes.dev as custom domain"
        location: "Vercel Project -> Settings -> Domains -> Add frexes.dev"
      - task: "Point DNS to Vercel (if not already done)"
        location: "Domain registrar -> Set nameservers to Vercel or add CNAME"
must_haves:
  truths:
    - "frexes.dev loads the site in a browser without errors"
    - "OG image preview shows correctly when URL is shared"
  artifacts:
    - path: "vercel.json"
      provides: "Vercel deployment config (if needed)"
  key_links:
    - from: "frexes.dev"
      to: "Vercel hosting"
      via: "DNS + Vercel deployment"
      pattern: "N/A — external"
---

<objective>
Deploy the site to frexes.dev via Vercel.

Purpose: Ship the portfolio live so visitors can access it at frexes.dev.
Output: Live site at https://frexes.dev with correct OG previews.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-launch/04-RESEARCH.md
@.planning/phases/04-launch/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to Vercel</name>
  <files>vercel.json</files>
  <action>
1. Ensure the project is pushed to GitHub (the frexes repo).

2. Attempt deployment via Vercel CLI: `bunx vercel --yes`
   - If CLI auth fails, create a `checkpoint:human-action` for the user to import the repo via Vercel dashboard instead.
   - Vercel auto-detects Astro and uses `astro build`. It also auto-detects `bun.lock` for Bun runtime.

3. If needed, create `vercel.json` with minimal config:
   ```json
   {
     "$schema": "https://openapi.vercel.sh/vercel.json",
     "framework": "astro"
   }
   ```
   Only create this if Vercel auto-detection fails.

4. After deployment succeeds, note the preview URL. Deploy to production: `bunx vercel --prod --yes`

5. Add custom domain frexes.dev if not already configured (this may require human action for DNS).
  </action>
  <verify>
    <automated>bunx vercel ls 2>/dev/null | head -5 && echo "Vercel project exists"</automated>
  </verify>
  <done>Site deployed to Vercel with a working preview URL. Production deployment triggered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live site at frexes.dev</name>
  <action>Human verifies the deployed site is working correctly at frexes.dev.</action>
  <verify>User confirms site loads, OG image works, all links functional.</verify>
  <done>frexes.dev approved as live and correct.</done>
  <what-built>Full site deployed to frexes.dev — Astro static site with self-hosted fonts, animations, and OG image.</what-built>
  <how-to-verify>
1. Visit https://frexes.dev — site loads with hero, app cards, about section, footer
2. Check browser console — no errors
3. Test on mobile (or responsive mode) — layout is correct
4. Paste https://frexes.dev into https://developers.facebook.com/tools/debug/ (or Twitter Card Validator) — OG image shows "Frexes" with tagline
5. Verify all links work: energyxp.app, progres.ing, X profile, LinkedIn
  </how-to-verify>
  <resume-signal>Type "approved" if site is live and correct, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
- https://frexes.dev loads without errors
- OG image preview correct on social media debugger
- All external links functional
- Mobile responsive layout intact
</verification>

<success_criteria>
frexes.dev is live, loads correctly, OG image previews work, all links functional.
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch/04-02-SUMMARY.md`
</output>
