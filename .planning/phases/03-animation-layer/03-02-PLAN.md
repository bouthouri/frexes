---
phase: 03-animation-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AppCard.tsx
  - src/components/AppsGrid.astro
  - src/pages/index.astro
  - src/styles/global.css
autonomous: true
requirements:
  - APPS-05
  - APPS-06

must_haves:
  truths:
    - "Hovering an app card produces a lift (y: -4) and scale (1.02) micro-interaction with spring physics"
    - "App cards have a continuously running ambient shimmer effect without user interaction"
    - "Each card's shimmer uses the app's unique accent color"
    - "All card animations stop when prefers-reduced-motion is enabled"
    - "Live app cards still link to their URLs; Coming Soon cards are not clickable"
  artifacts:
    - path: "src/components/AppCard.tsx"
      provides: "React island with m.div hover + CSS ambient shimmer"
      exports: ["AppCard"]
    - path: "src/components/AppsGrid.astro"
      provides: "Grid rendering AppCard as React island via AppsGridInner"
      contains: "client:visible"
    - path: "src/styles/global.css"
      provides: "card-shimmer keyframes and app-card ::before pseudo-element"
      contains: "@keyframes card-shimmer"
  key_links:
    - from: "src/components/AppCard.tsx"
      to: "src/data/apps.ts"
      via: "App type import"
      pattern: "import.*App.*from.*data/apps"
    - from: "src/components/AppCard.tsx"
      to: "src/lib/motion.ts"
      via: "usePrefersReducedMotion hook"
      pattern: "usePrefersReducedMotion"
    - from: "src/components/AppCard.tsx"
      to: "src/components/motion/LazyMotionProvider.tsx"
      via: "Wrapped by LazyMotionProvider in AppsGrid"
      pattern: "LazyMotionProvider"
    - from: "src/components/AppsGrid.astro"
      to: "src/components/AppCard.tsx"
      via: "React island with client:visible"
      pattern: "client:visible"
---

<objective>
Convert AppCard to a React island with Framer Motion hover interaction and CSS ambient shimmer.

Purpose: Cards feel alive with continuous shimmer and respond to hover with spring-physics lift+scale.
Output: AppCard.tsx (new React component), updated AppsGrid.astro, CSS shimmer keyframes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-animation-layer/03-RESEARCH.md

@src/components/AppCard.astro
@src/components/AppsGrid.astro
@src/components/StatusBadge.astro
@src/data/apps.ts
@src/lib/motion.ts
@src/components/motion/LazyMotionProvider.tsx
@src/pages/index.astro
@src/styles/global.css
</context>

<interfaces>
<!-- App type from src/data/apps.ts -->
```typescript
export type AppStatus = "live" | "coming-soon";
export interface App {
  id: string;
  name: string;
  tagline: string;
  status: AppStatus;
  url: string | null;
  icon: string;
  accentColor: string;
}
```

<!-- LazyMotionProvider from src/components/motion/LazyMotionProvider.tsx -->
```typescript
export function LazyMotionProvider({ children }: { children: ReactNode }): JSX.Element;
```

<!-- usePrefersReducedMotion from src/lib/motion.ts -->
```typescript
export function usePrefersReducedMotion(): boolean;
```

<!-- StatusBadge.astro (must be inlined in React version) -->
Status badge logic: if status === "live" -> green bg + "Live" text, else amber bg + "Coming Soon" text.
Classes: "inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium"
Live: "bg-green-100 text-green-800"
Coming Soon: "bg-amber-100 text-amber-800"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create AppCard.tsx React island with hover + shimmer setup</name>
  <files>src/components/AppCard.tsx, src/styles/global.css</files>
  <action>
**Create `src/components/AppCard.tsx`:**

1. Import `m` from `motion/react`, `usePrefersReducedMotion` from `../lib/motion`, and `App` type from `../data/apps`.

2. Accept `{ app: App }` props.

3. Render `m.div` as the outer wrapper:
   - `whileHover`: `{ y: -4, scale: 1.02 }` — but only if `!prefersReduced` (pass `undefined` if reduced)
   - `transition`: `{ type: "spring", stiffness: 300, damping: 20 }`
   - `className`: `"app-card rounded-2xl border border-border bg-background p-6 relative overflow-hidden"`
   - `style`: `{ "--card-accent": app.accentColor } as React.CSSProperties` (feeds the CSS shimmer)

4. Inside m.div, use dynamic tag pattern:
   - If `app.url` exists, render an `<a>` with `href={app.url}`, `target="_blank"`, `rel="noopener noreferrer"`
   - Otherwise render a `<div>`
   - Inner content class: `"flex items-start gap-4"`

5. Card content (matching current AppCard.astro exactly):
   - `<img>` with `src={app.icon}`, `alt`, `width={48}`, `height={48}`, `className="rounded-xl"`
   - Name + inline StatusBadge: `<div className="flex items-center gap-2">` with `<h3>` and status span
   - StatusBadge inlined as a `<span>` — use `app.status === "live"` to toggle green/amber classes and "Live"/"Coming Soon" text. Copy exact classes from StatusBadge.astro.
   - Tagline: `<p className="mt-1 text-sm text-muted-foreground">{app.tagline}</p>`

6. Export as named export: `export function AppCard({ app }: { app: App })`

**Add to `src/styles/global.css`** (after existing content, and after any gradient-shift keyframes if Plan 01 ran first):

1. `@keyframes card-shimmer` — oscillates opacity between 0.03 and 0.08 over 4s.

2. `.app-card` — `position: relative; overflow: hidden;` (redundant with Tailwind but ensures CSS pseudo-element works).

3. `.app-card::before` pseudo-element:
   - `content: ''`
   - `position: absolute; inset: 0; border-radius: inherit;`
   - `background: linear-gradient(135deg, var(--card-accent, #6366f1) 0%, transparent 60%)`
   - `animation: card-shimmer 4s ease-in-out infinite`
   - `pointer-events: none`

4. Add to the `@media (prefers-reduced-motion: reduce)` block:
   - `.app-card::before { animation: none; opacity: 0.05; }`

Do NOT use `motion.div` — use `m.div` (tree-shakeable, requires LazyMotion ancestor).
Do NOT import StatusBadge.astro — inline the badge JSX.
  </action>
  <verify>
    <automated>cd /Users/bouthouri/work/frexes && bun run build</automated>
  </verify>
  <done>AppCard.tsx exists with m.div hover interaction, inline status badge, and CSS custom property for shimmer accent color. global.css has card-shimmer keyframes with reduced-motion override.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AppsGrid to render React AppCard island</name>
  <files>src/components/AppsGrid.astro, src/pages/index.astro</files>
  <action>
**Update `src/components/AppsGrid.astro`:**

1. Change imports: replace `AppCard.astro` import with imports of `AppCard` from `./AppCard.tsx` and `LazyMotionProvider` from `./motion/LazyMotionProvider`.

2. Keep the `apps` import from `../data/apps`.

3. Render structure: Keep the outer `<section>` and `<h2>` as Astro (static HTML, zero JS). But the grid of cards needs to be a React island for m.div to work.

4. Create an inline React wrapper or import approach. Recommended approach — create a small wrapper directly in AppsGrid.astro:
   ```astro
   <section id="apps" class="...">
     <h2 class="...">What I'm Building</h2>
     <LazyMotionProvider client:visible>
       <div class="grid grid-cols-1 gap-4 md:grid-cols-2">
         {apps.map((app) => <AppCard app={app} />)}
       </div>
     </LazyMotionProvider>
   </section>
   ```

   Note: Astro can render React components with `client:visible` — the `LazyMotionProvider` becomes the hydration boundary. AppCard renders as children inside it. Both are React components so this works.

   If Astro doesn't allow mapping React children inside an Astro file this way, create a `AppsGridInner.tsx` React component that accepts `{ apps: App[] }` and renders `<LazyMotionProvider><div className="grid...">{apps.map(...)}</div></LazyMotionProvider>`, then use `<AppsGridInner apps={apps} client:visible />` in AppsGrid.astro.

**Update `src/pages/index.astro`:**

1. Remove the standalone `<LazyMotionProvider client:visible>` placeholder at the bottom — it's no longer needed since LazyMotionProvider is now inside AppsGrid.

2. Remove the import of `LazyMotionProvider` from the frontmatter.

3. Keep everything else (BaseLayout, Hero, About, Footer imports and usage) unchanged.
  </action>
  <verify>
    <automated>cd /Users/bouthouri/work/frexes && bun run build</automated>
  </verify>
  <done>AppsGrid renders AppCard as React island wrapped in LazyMotionProvider with client:visible. Old placeholder LazyMotionProvider removed from index.astro. All 4 app cards visible with correct content, hover interaction works, ambient shimmer visible.</done>
</task>

</tasks>

<verification>
- `bun run build` succeeds with no errors
- `bun run preview` — all 4 app cards render with correct name, icon, tagline, status badge
- Hovering a card produces visible lift + scale with spring feel
- Cards show subtle shimmer animation without hovering
- Each card's shimmer tint matches its accent color
- Energy XP and Progres cards link to external URLs
- Voila and Karv cards are not clickable links
- Toggling prefers-reduced-motion stops hover animation and shimmer
</verification>

<success_criteria>
App cards have spring-physics hover micro-interaction and continuous ambient shimmer. All animations respect reduced motion. Card content and links preserved from Phase 2.
</success_criteria>

<output>
After completion, create `.planning/phases/03-animation-layer/03-02-SUMMARY.md`
</output>
