---
phase: 03-animation-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Hero.astro
  - src/styles/global.css
autonomous: true
requirements:
  - HERO-02

must_haves:
  truths:
    - "Hero background has a visible animated gradient on page load without scrolling"
    - "Gradient animation is subtle and ambient — slow-moving, low opacity, not distracting"
    - "Enabling prefers-reduced-motion stops the gradient animation without breaking layout"
  artifacts:
    - path: "src/components/Hero.astro"
      provides: "Ambient gradient background div behind hero content"
      contains: "hero-gradient"
    - path: "src/styles/global.css"
      provides: "gradient-shift keyframes and reduced-motion media query"
      contains: "@keyframes gradient-shift"
  key_links:
    - from: "src/components/Hero.astro"
      to: "src/styles/global.css"
      via: "hero-gradient CSS class"
      pattern: "hero-gradient"
---

<objective>
Add ambient animated gradient background to the Hero section.

Purpose: Make the hero feel alive and premium with an always-running gradient animation (no scroll trigger).
Output: Hero.astro updated with gradient div, global.css with keyframes + reduced-motion handling.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-animation-layer/03-RESEARCH.md

@src/components/Hero.astro
@src/styles/global.css
</context>

<interfaces>
<!-- Current Hero.astro structure -->
```astro
<section class="flex flex-col items-center text-center py-24 sm:py-32 lg:py-40 px-4 sm:px-6 max-w-4xl mx-auto">
  <h1>Frexes</h1>
  <p>Building apps that actually help you get things done</p>
</section>
```

<!-- Current global.css theme tokens -->
```css
@theme {
  --color-background: #ffffff;
  --color-foreground: #0a0a0a;
  --color-accent: #000000;
}
```

<!-- App accent colors for gradient palette -->
Energy XP: #6366f1, Progres: #22c55e, Voila: #f59e0b, Karv: #ec4899
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS keyframes for hero gradient animation</name>
  <files>src/styles/global.css</files>
  <action>
Add to global.css after the @theme block:

1. `@keyframes gradient-shift` — animates `background-position` from `0% 50%` to `100% 50%` and back over a full cycle.

2. `.hero-gradient` class:
   - `background: linear-gradient(-45deg, #6366f1, #ec4899, #22c55e, #f59e0b)` (the 4 app accent colors)
   - `background-size: 400% 400%`
   - `animation: gradient-shift 15s ease infinite`

3. `@media (prefers-reduced-motion: reduce)` block:
   - `.hero-gradient { animation: none; background-position: 0% 50%; }`

Keep existing @import and @theme untouched.
  </action>
  <verify>
    <automated>cd /Users/bouthouri/work/frexes && bun run build</automated>
  </verify>
  <done>global.css contains gradient-shift keyframes, .hero-gradient class, and reduced-motion override</done>
</task>

<task type="auto">
  <name>Task 2: Wire gradient background into Hero.astro</name>
  <files>src/components/Hero.astro</files>
  <action>
Update Hero.astro to add the ambient gradient behind the content:

1. Wrap the existing section content — add `relative overflow-hidden` to the section element.

2. Add a gradient div as first child inside the section:
   ```html
   <div class="hero-gradient absolute inset-0 -z-10 opacity-20 blur-3xl" aria-hidden="true"></div>
   ```
   - `absolute inset-0` fills the section
   - `-z-10` places it behind content
   - `opacity-20` keeps it subtle
   - `blur-3xl` creates a soft mesh feel
   - `aria-hidden="true"` for accessibility

3. Wrap remaining content in a `<div class="relative">` to ensure text stays above gradient.

Do NOT change the existing text content, font sizes, spacing, or any classes on h1/p elements.
  </action>
  <verify>
    <automated>cd /Users/bouthouri/work/frexes && bun run build</automated>
  </verify>
  <done>Hero section shows animated gradient background behind text. Gradient uses app accent colors at low opacity with blur. Animation stops with prefers-reduced-motion enabled.</done>
</task>

</tasks>

<verification>
- `bun run build` succeeds with no errors
- `bun run preview` — hero section shows subtle moving gradient behind "Frexes" heading
- Toggling prefers-reduced-motion in System Settings stops the animation
</verification>

<success_criteria>
Hero has a visible, subtle ambient gradient animation on page load. No scroll trigger. Reduced motion respected.
</success_criteria>

<output>
After completion, create `.planning/phases/03-animation-layer/03-01-SUMMARY.md`
</output>
